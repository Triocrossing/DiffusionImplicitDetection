# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: ladde
  - override /model: dire_resnet
  - override /callbacks: default
  - override /trainer: default

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["ladde", "test"]

seed: null

trainer:
  min_epochs: 50
  max_epochs: 100

data:
  batch_size: 256
  train_test_split: [0.75, 0.15, 0.1]
  mode: "cofeat"
  
logger:
  wandb:
    tags: ${tags}

callbacks:
  model_checkpoint:
    monitor: "val/acc"
    mode: "max"
    save_last: True
    auto_insert_metric_name: False
    save_top_k: 2

model:
  net:
    _target_: src.models.components.resnet.resnet50patch
    num_classes: 1
  optimizer:
    weight_decay: 0.001